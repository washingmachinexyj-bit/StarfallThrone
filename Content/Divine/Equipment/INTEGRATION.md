# Divine equipment worker

Assets: all 12 weapon attacks reuse Weapon0..11 through native SpriteBatch plus geometric telegraph/trails; minions use the three separately generated Minion0..2.png creature sprites (64x64), agreed with main/gear artist. Armor procs reuse Expert0..2; buffs reuse weapon icons. Existing agreed armor native equip sheets required unchanged.

Integration API: namespace StarfallThrone.Content.Divine.Equipment. DivineEquipmentPlayer.ReceivePacket(BinaryReader,int) owns packet41 (after root consumes packet byte). DivineEquipmentValidation.Data()/Run() are opt-in smoke entry points. Concrete items use frozen DivineWeaponN, DivineArmorN, DivineExpertN names. No new main/root changes needed except routing packet41 and calling tests.

Tests now implemented. Main calls Data()/Run() on isolated smoke client. Runtime logs: DIVINE_EQUIPMENT_DATA_PASS, 12 DIVINE_WEAPON_RUNTIME_PASS, DIVINE_WEAPONS_RENDER_PASS, DIVINE_EQUIPMENT_ABILITIES_PASS, DIVINE_EQUIPMENT_BOUNDARIES_PASS, DIVINE_ARMOR_RENDER_PASS, DIVINE_EQUIPMENT_RUNTIME_PASS. Images: outputs/divine-weapons-runtime.png and outputs/divine-armor-runtime.png (12 loadouts x3 poses). Tests use Player.PickAmmo, CheckMana, Projectile.AI/Damage, Player.Hurt/Teleport and native PlayerRenderer, with complete fixture restoration.

Packet41: mode0 skill request; mode1 server skill state; modes2/3 monotonic cooldown snapshots; mode4 owner hit evidence for server heat award (target index and owned projectile identity, no heat value); mode5 server heat acknowledgement. Server validates current armor, active owner/target, primary projectile geometry (48px network tolerance), LOS, and 6-tick award budget. Stale evidence is deliberately rejected. Normal vanilla player-owned damage remains client-authoritative; this is bounded validation, not anti-cheat or proof of real two-client play. Real multiplayer latency/heat-credit behavior is not tested by the isolated harness.

Minor explicitly documented numbers resolving underspecified design: furnace active window8s then24s recovery =32s total from activation; morning dew shot fixed18 base damage; heat5 per0.1s with25 loss on damage; return range100tiles; refund60 at1HP/0.1s after6s wait. Buffs use DivineMinionBuff0..2.

This worker does not run full builds or game launches. Main must compile and run both installed/not-installed BossChecklist paths before release. Awaiting smoke findings; no test result claimed until main actually runs it.

First smoke repair: weapon1 empowered drops assertion exposed a fixture outer-loop omission. The installed engine IL confirms Projectile.Damage calls CombinedHooks.OnHitNPCWithProj then decrements penetration (IL_421b..4277), whereas Projectile.Update separately kills penetrate==0 (IL_1d1d..1d26). The fixture now performs that outer-loop despawn immediately after native Damage returns. Previously one-hit arrows kept moving into later dummies and resetting the same-target counter. Gameplay unchanged; tests strengthened to assert every arrow's exact target/count/ready state and untouched rear dummies. Existing empowered-drop assertion remains.

Second smoke repair: all12 native weapons and abilities passed. The menu packet fixture had no negotiated Mod.netID, so its real server acknowledgement threw in GetPacket. Test-only PacketFixture now temporarily provides netID0/netMods registration and replaces RemoteClient sockets with an in-memory ISocket capturing real ModPacket.Send frames. No connection/listener/external I/O is allowed; identities, tables, original RemoteClient references and broadcast flags restore in finally/Dispose. Tests require exactly two native framed replies (cooldown mode3 and heat mode5). No production handler or gameMenu bypass changed. Bow1/gun5/magic6 capture tick moved from8 to1 to catch their visible projectiles before impact. Frozen pending main smoke.

Menu buffer follow-up: PacketFixture now snapshots the complete NetMessage.buffer reference array, provides newly constructed MessageBuffer objects for every index, and restores the original references (including null entries) in Dispose. This is a complete native transport fixture, not null-forgiving/skip logic; existing live-buffer contents are never mutated.
